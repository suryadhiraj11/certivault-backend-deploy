spring.application.name=certitrack-backend

# Railway MySQL Database
spring.datasource.url=jdbc:mysql://shuttle.proxy.rlwy.net:46753/railway?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=qNdsseTcZJsgcKMoavJxgnmABjVipfLB
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration,org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true

# IMPORTANT: let Spring auto-detect dialect (do NOT add MySQL8Dialect)
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect ❌ REMOVE

# Server port (required for Railway)
server.port=${PORT:8080}

# Logging (optional)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
